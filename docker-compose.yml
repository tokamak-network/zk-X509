services:
  anvil:
    image: ghcr.io/foundry-rs/foundry@sha256:9e591221051112fe0bb530abcaba67f43f01ebbd12a94a8632d570d5e065a8bf
    entrypoint: ["anvil", "--host", "0.0.0.0"]
    ports:
      - "8545:8545"
    healthcheck:
      test: ["CMD", "cast", "block-number", "--rpc-url", "http://localhost:8545"]
      interval: 2s
      timeout: 5s
      retries: 10

  deployer:
    image: ghcr.io/foundry-rs/foundry@sha256:9e591221051112fe0bb530abcaba67f43f01ebbd12a94a8632d570d5e065a8bf
    user: root
    depends_on:
      anvil: { condition: service_healthy }
    volumes:
      - ./contracts:/contracts
      - ./scripts:/scripts:ro
      - deployer-output:/shared
      - ./.docker-addresses.json:/host-root/.docker-addresses.json
    working_dir: /contracts
    entrypoint: ["sh", "/scripts/deploy-local.sh"]

  backend:
    build: ./backend
    depends_on:
      deployer: { condition: service_completed_successfully }
    ports:
      - "4000:4000"
    volumes:
      - deployer-output:/shared:ro
    environment:
      - PORT=4000
      - CORS_ORIGIN=http://localhost:3000
      - RPC_URL=http://anvil:8545

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "3000:3000"
    volumes:
      - ./scripts:/scripts:ro
      - deployer-output:/shared:ro
    entrypoint: ["sh", "/scripts/frontend-entrypoint.sh"]
    environment:
      - NEXT_PUBLIC_RPC_URL=http://localhost:8545
      - NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
      - NEXT_PUBLIC_CHAIN_ID=31337

volumes:
  deployer-output:
